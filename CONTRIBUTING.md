# Contributing to Nexus Foundry

1. Atomic Components: UI components must live in `/packages/shared/`.
2. Naming: Use lowercase kebab-case for all folders/files.
3. API Integrity: New tools require a corresponding `api/` route in `apps/tools/`.
4. Deploy: All pushes to `main` trigger automated Vercel production builds.
